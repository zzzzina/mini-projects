import styles from '../../styles/landing/Todo.module.scss';

const Footer = () =>{
    return (
        <div className={styles.footer}>
            푸우우우우우우우우터어어어어어어어
        </div>
    )
}

export default Footer;